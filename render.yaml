services:
  - type: web
    name: voltguard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/dashboard.py
    envVars:
      - key: PORT
        value: 7860